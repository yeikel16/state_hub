import 'package:flutter/material.dart';

class FavoritePropertiesPage extends StatelessWidget {
  const FavoritePropertiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favorite Properties Page'),
    );
  }
}
